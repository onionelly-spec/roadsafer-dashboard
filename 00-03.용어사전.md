# 프로젝트 용어사전

본 프로젝트에서 **DB 컬럼명·테이블 접두사·공통 필드** 등에 사용하는 용어를 **한글명 · 영문명 · 영문약어** 표로 정리했다.

로드세이프티 **기획서·도메인 문서**와의 맞춤은 **§10-2**, **§13** 을 본다. (워크스페이스 `docs/00.프로젝트/00-04` 등과 상호 참조.)

---

## 1. 일시 · 일자

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 일시 | date time | dtm |
| 일자 | date | dt |
| 등록 일시 | register date time | reg_dtm |
| 수정 일시 | modify date time | mod_dtm |
| 생년월일 | birthday | birthday |
| 기간 시작 | from | dtm_from |
| 기간 종료 | to | dtm_to |

> `dtm_from` / `dtm_to`는 검색 조건 필드에 사용하는 접미사. 예: `rev_dtm_from`, `rev_dtm_to`.  
> Java VO에서는 camelCase로 `revDtmFrom`, `revDtmTo`.

---

## 2. 등록 · 수정 · 상세

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 등록 | register | reg |
| 수정 | modify | mod |
| 상세 | detail | detail |
| 등록자 순번 | register sequence | reg_seq |
| 수정자 순번 | modify sequence | mod_seq |
| 작업 구분 | operation | operation |

> `operation`: CRUD 작업 유형을 나타내는 컬럼. 값은 `I`(INSERT) · `U`(UPDATE) · `D`(DELETE).

---

## 3. 식별자 · 키

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 순번 | sequence | seq |
| 식별자 | identifier | id |
| 번호 | number | no |
| 엔티티 | entity | entity |

> `entity_id`: 변경 이력 등 공통 테이블에서 대상 행의 PK를 문자열로 저장할 때 사용.

---

## 4. 여부 · 플래그

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 여부 | yes/no | yn |
| 사용 여부 | use yes/no | use_yn |
| 삭제 여부 | delete yes/no | del_yn |

---

## 5. 정렬 · 순서

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 정렬 순서 | order | ordr |

---

## 5-1. 조회 · 변경(이력)

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 조회 일시 | view date time | view_dtm |
| 변경 일시 | revision date time | rev_dtm |
| 변경자 순번 | revision sequence | rev_seq |
| 이력 | history | hist |
| 이전(수정 전) | before | before |
| 이후(수정 후) | after | after |
| 스냅샷 | snapshot | snapshot |
| 접근·접속 | access | access |

> `snapshot_before` / `snapshot_after`: 변경 전·후 행 데이터를 JSON으로 저장하는 컬럼.  
> `access`: 페이지 접근(`access_type`, `accessed_dtm`) 및 개인정보 조회(`access_result`, `privacy_access_seq`) 양쪽에서 사용.

---

## 6. 도메인 · 테이블 접두사

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 사용자 | user | usr |
| 회원 | member | mber |
| 관리자 | admin | adm |
| 시스템 | system | sys |
| 통계 | statistics | sts |
| 마이페이지 | my page | mypage |

---

## 6-1. 도메인(기능) 접두사 보조

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 메뉴 | menu | menu |
| 권한 | role | role |
| 작업 권한 | role | role |
| 온오프 | on off | onoff |
| 개인정보 | privacy | privacy |
| 개인정보 조회 목적 | privacy access purpose | access_purpose |
| 개인정보 조회 이력 | privacy access log | privacy_access |

---

## 7. 검색 · 목록(페이징)

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 검색 조건 | search condition | searchCondition |
| 검색어 | search keyword | searchKeyword |
| 검색 타입 | search type | searchType |
| 페이지(목록) | page | page |
| 갯 수 | count | cnt |

---

## 8. 기타 관용어

### 8-1. 계정 · 인증

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 로그인 | login | login |
| 비밀번호 | password | password |
| 세션 | session | session |
| 시도 | attempt | attempt |
| 잠금 | lock | lock |

### 8-2. 전화번호 · 연락처

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 전화번호 | telephone number | tel |
| 전화번호 뒷 4자리 | telephone number tail | tel_no_tail |
| 이메일 | email | email |

### 8-3. 회원 개인정보

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 이름 | name | nm |
| 주소 | address | address |
| 우편번호 | postal code | post_code |
| 성별 | gender | gender |
| 생년월일 | birthday | birthday |

> `birthday`는 Section 1(일시·일자)과 중복 등재. DATE 타입으로 DB 컬럼명 그대로 사용한다.

### 8-4. 공통코드

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 코드 | code | code |
| 그룹 코드 | group code | group_code |
| 상세 코드 | detail code | detail_code |

### 8-5. 일반 패턴

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| 유형 | type | type |
| 목적 | purpose | purpose |
| 결과 | result | result |
| 대상 | target | target |
| 기타 | etc | etc |
| 경로 | path | path |
| 설명 | description | description |
| 부모 순번 | parent sequence | parent_seq |
| 테이블명 | table name | table_nm |
| 컴포넌트 | component | (없음) |
| 유틸 | utility | util |
| 에러 | error | err |

### 8-6. HTTP · 통계 전용

> 아래 용어는 `sts_` 접두사 통계 테이블에서만 사용한다.

| 한글명 | 영문명 | 영문약어 |
|--------|--------|----------|
| IP 주소 | IP address | ip |
| 유저에이전트 | user agent | user_agent |
| 리퍼러 | referrer | referrer |

---

## 9. 목록 · 상세 · 등록 · 수정(URL 경로)

화면/API URL 규칙. **페이지 진입**은 `-form` 접미사, **API**는 동작명만 사용. 도메인·리소스는 용어사전 약어(adm, mber, usr 등) 사용. **등록=reg, 수정=mod, 상세=detail** 로 URL 경로 통일.

**페이지 진입(화면)**

| 한글명 | 영문명 | URL 경로 | 비고 |
|--------|--------|----------|------|
| 목록 | list | `/**/**/list-form` | 목록 화면 |
| 상세보기 | detail | `/**/**/detail-form/{id}` | 단건 조회 화면 |
| 등록 | register(reg) | `/**/**/reg-form` | 등록 폼 화면 |
| 수정 | modify(mod) | `/**/**/mod-form/{id}` | 수정 폼 화면 |

**데이터(API)** — 접두사 /api 없음. 같은 도메인/리소스 경로에서 list, detail, reg, mod 로 구분.

| 한글명 | 영문명 | URL 경로 | HTTP | 비고 |
|--------|--------|----------|------|------|
| 목록 | list | `GET /**/**/list` | GET | 목록 조회 |
| 단건 조회 | detail | `GET /**/**/detail/{id}` | GET | 상세 조회 |
| 등록 | register(reg) | `POST /**/**/reg` | POST | 등록 |
| 수정 | modify(mod) | `PUT /**/**/mod/{id}` | PUT | 수정 |
| 삭제 | delete | `DELETE /**/**/{id}` | DELETE | 삭제 |

---

*DB·스키마 작업 시 `docs/00.프로젝트/00-01.프로젝트규칙.md` 4.2·4.4와 함께 참고할 것.*

---

## 10. 퍼블리싱 도메인 용어 (추가)

본 대시보드 퍼블리싱에서 도출되어 추가된 도메인 및 컬럼 용어입니다.

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| 공사 | construction | construction | |
| 공사 유형 | construction type | construction_type | |
| 발주 업체 | order company | order_company | |
| 장비 | equipment | equip | |
| 씨인카 | sinca | sinca | |
| 알림 | notification | noti | |
| 광고 | advertisement | ad | |
| 고정(공지 등) | pin | pin | `pin_yn` |
| 썸네일 | thumbnail | thumb | |
| 읽음 | read | read | `read_yn` |
| 담당자 | manager | manager | |
| 승인자 | approver | approver | |
| 이미지 | image | img | |
| 로고 | logo | logo | |
| 파일 | file | file | |
| 작업 | work | work | |
| 도로 | road | road | |
| 방향 | direction | direction | |
| 차단 구간 | block section | block_section | |
| 신호수 | signaler | signaler | |
| 소유주 | owner | owner | |
| 의견 | opinion | opinion | |
| 장터 | market | market | |
| 수량 | quantity | qty | |
| 규격 | specification | spec | |
| 가격 | price | price | |
| 은행 | bank | bank | |
| 통장 | bankbook | bankbook | |
| 권역 | region | region | |
| 사업자번호 | business number | biz_no | |
| 대표자 | ceo | ceo | |
| 승인 | approve | approve | |
| 반려 | reject | reject | |
| 취소 | cancel | cancel | |
| 대기 | pending | pending | |
| 단가 | unit price | unit_price | |
| 긴급 여부 | urgent yes/no | urgent_yn | |
| 차단 시작 시간 | block start time | block_start_time | |
| 주간 근무 여부 | day shift yes/no | day_shift_yn | |
| 야간 근무 여부 | night shift yes/no | night_shift_yn | |

### 10-2. 로드세이프티 기획서 도메인 보강 (약어)

기획서·`docs/10.DB설계` 초안에만 흩어져 있던 호칭을 **여기 약어로 고정**한다. §10과 중복되는 항목은 §10을 따르고, 이 표는 **로드세이프티 전용 보강**이다.

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| 발주기업 | order corporation | order_corp | §10 `order_company` 와 동의. DB·VO 예: `order_corp_id` |
| 차단업체 | block company (vendor) | block_corp | 기획서 **차단기업**과 동일 개념. UI에서 ‘업체’로 쓰는 경우 |
| 차단신청 | block request | block_req | B4-2 / M1-2-1. 테이블 예: `rs_construction_block_request` |
| 차단정보 | block information | block_info | 화면 탭·도메인 묶음 명칭(M1-2 등) |
| 차단불이행 | non performance | non_perf | M2. 테이블 예: `rs_non_performance` |
| 매칭 | matching | match | 차단기업과 공사 연결 완료(동사·상태 전이) |
| 시연회 | rehearsal | rehearsal | 공사 부가 일정 |
| 공사일 | work day | work_day | 캘린더 다중 선택 일자 |
| 일자별 진행 | daily progress | daily_prog | 일자별 완료·금액 |
| 품목(라인) | item line | item_line | TMA1/TMA3/PLCS 라인 |
| 발주 담당자 | order contact | order_contact | 발주기업 측 담당자명·연락처 |
| 도로구분 | road class | road_class | 라디오 구분값 |
| 길 구분 | lane class | lane_class | 라디오 구분값 |
| 주간 근무 | day shift | day_shift | `is_day_shift` 등과 대응 |
| 야간 근무 | night shift | night_shift | `is_night_shift` 등과 대응 |
| 긴급 공사 | urgent construction | urgent | `is_urgent`, 목록 상단 고정과 연계 |
| 취소 정산 | cancel settlement | cancel_stlm | M3-1 등 |
| 매출 | revenue | revenue | M9·B3 매출 |
| 알림 템플릿(카카오) | notification template | noti_tpl | 코드값 **P01~P11** (`docs/00-04` 표와 동일) |
| 통합 운영자 | integrated operator | intg_opr | 기업·직원 승인 등 **운영자 일괄** 처리 문맥 |
| 최고관리자 | super administrator | super_adm | 역할 예: `ROLE_SUPER_ADMIN` |
| 콜센터 | call center | call_ctr | 역할 예: `ROLE_CALL_CENTER` |
| TMA 카드 | TMA card | tma | 품목 카드. 종류 코드: TMA1, TMA3, PLCS 등은 `tma_kind` 등으로 저장 |
| 커뮤니티 | community | community | M8. 게시글·댓글 도메인 |
| 댓글 | comment | comment | 무한중첩 등은 DB 설계서 참고 |
| 공지 | notice | notice | M5. `noti`(알림 발송)와 구분: 공지 콘텐츠 본체 |

---

## 11. 퍼블리싱 공통 CSS 클래스 (추가)

본 대시보드 퍼블리싱에서 `rss-common.css`에 정의된 공통 UI 컴포넌트 클래스 목록입니다.

| 클래스명 | 용도 | 비고 |
|---|---|---|
| `.rss-select` | 드롭다운 select 공통 스타일 | 검색 툴바·폼 모두 사용 |
| `.rss-input` | 텍스트 input 공통 스타일 | 폼 입력 필드 |
| `.rss-textarea` | textarea 공통 스타일 | 폼 본문 입력 |
| `.rss-search-wrap` | 검색 input + 버튼 래퍼 | 목록 툴바 검색 영역 |
| `.rss-board-toolbar` | 목록 툴바 (검색+버튼 행) | 좌우 분리 레이아웃 |
| `.rss-link` | 링크 텍스트 공통 스타일 | 기본 모노톤, 호버 옐로톤 |
| `.rss-table` | 목록형 테이블 공통 스타일 | th 상단 헤더 구조 |
| `.rss-table--detail` | 키/값 형태 상세 테이블 변형 | th 좌측 레이블 구조 |
| `.rss-col-nm` | 공사명·제목 등 이름 열 너비 | `min-width:160px` |
| `.rss-col-period` | 기간 열 너비 | `min-width:200px; white-space:nowrap` |
| `.rss-col-equip` | 장비 열 너비 | `min-width:120px` |
| `.rss-badge` | 상태 배지 공통 스타일 | 색상 변형 클래스 조합 |
| `.rss-btn` | 버튼 공통 스타일 | 색상 변형 클래스 조합 |
| `.rss-card` | 카드 컨테이너 공통 스타일 | 흰 배경·라운드·그림자 |
| `.rss-modal-overlay` | 모달 오버레이 | 전체화면 반투명 배경 |
| `.rss-modal` | 모달 박스 | 중앙 정렬 카드 |

> 위 클래스를 사용하면 `rss-common.css` 한 곳만 수정해도 전체 페이지에 일괄 반영됩니다.  
> 개별 페이지 `<style>` 태그에 동일 속성을 재정의하는 것을 **금지**합니다.

---

## 12. 로그인 유형 구분 (추가)

| 한글명 | 영문명 | 값 | 비고 |
|---|---|---|---|
| 차단기업 | block company | `company` | `loginType=company` |
| 차단기업 직원 | block company employee | `employee` | `loginType=employee` |

> `loginType` 값에 따라 금액 열 표시 여부, 씨인카 수량 표시 여부, 월매출액 표시 여부가 분기됩니다.  
> Java: `th:if="${loginType == 'company'}"` / `th:if="${loginType == 'employee'}"`

---

## 13. 기획서(로드세이프티) ↔ 용어사전 통일표

기획서·도메인 MD(`docs/00.프로젝트/00-04`, `00-05`, `docs/10.DB설계` 등)에서 쓰는 말을 **본 용어사전의 한글·약어**로 모은다. 구현·DDL 작성 시 **§6 테이블 접두사**, **§3 PK `*_seq`**, **공통 필드 `reg_dtm` 등**은 `00-01.프로젝트규칙.md` §3과 함께 적용한다.

### 13-1. 역할·회원 (기획 표기 → 통일)

| 기획서·도메인 문서 표기 | 통일 한글명 | 영문 / 약어 | 비고 |
|-------------------------|-------------|-------------|------|
| 기업회원 | 차단기업(회원) | user **usr** / member **mber** | §12 `loginType=company`. VO·패키지는 `UsrMber*` 등 §2 규칙 |
| 차단기업(기업회원) | 위와 동일 | `BLOCK_CORP` 등 코드값과 병기 | |
| 차단기업 직원(부서) | 차단기업 직원 | `BLOCK_STAFF` / §12 `employee` | TMA·승인 주체가 기업과 다름(00-04) |
| 발주 담당, 발주기업 담당자 | 발주 담당자 | `order_contact` | §10-2 |
| 최고관리자 | 최고관리자 | `super_adm` | 운영 M11 |
| 콜센터 | 콜센터 | `call_ctr` | 운영 권한 제한 역할 |
| 통합 운영자(승인) | 통합 운영자 | `intg_opr` | B1 등 **운영 승인** 주체. `adm` 영역 API로 모델링 |

### 13-2. 공사·차단 (기획 표기 → 통일)

| 기획서·도메인 문서 표기 | 통일 한글명 | 영문약어 | 비고 |
|-------------------------|-------------|----------|------|
| 공사 | 공사 | `construction` | §10 |
| 공사 정보(B4) | 공사(매칭전 목록) | `construction` | 상태 필터와 연계 |
| 내 공사 현황(B3) | 공사 | `construction` | |
| 차단 신청(B4-2) | 차단신청 | `block_req` | §10-2 |
| 차단 정보, 차단정보 탭 | 차단정보 | `block_info` | |
| 차단 매칭 완료 | 매칭 완료 | `match` | 상태: 예정 계열로 전이 |
| 차단불이행(M2) | 차단불이행 | `non_perf` | 신규 공사·`origin` 연계 |
| 취소 내역·금액(M3) | 취소 정산 | `cancel_stlm` | §10-2 |
| 긴급 공사 | 긴급 공사 | `urgent` | §10-2, 단가·목록 정렬 |
| 권역 | 권역 | `region` | §10, 최대 개수 등은 기획 화면 문서 |
| 시연회 | 시연회 | `rehearsal` | §10-2 |
| TMA1 / TMA3 / PLCS | 품목(TMA) 종류 | `tma` + `tma_kind` | §10-2 |
| 씨인카 | 씨인카 | `sinca` | §10 |
| 신호수 | 신호수 | `signaler` | §10 |
| 커뮤니티(M8) | 커뮤니티 | `community` | §10-2 |

### 13-3. 공사 상태(기획 Enum ↔ 한글)

기획·구현에서 **동일 값**으로 쓴다. DB는 `*_ty` 또는 공통코드 `*_cd` 팀 선택(00-01 §3.2).

| 코드(예: Enum) | 기획서 한글 | 비고 |
|------------------|-------------|------|
| `MATCHING_BEFORE` | 매칭전 | B4 목록 노출 |
| `SCHEDULED` | 예정 | 차단 매칭 후 |
| `IN_PROGRESS` | 진행중 | |
| `CANCELLED` | 취소 | |
| `COMPLETED` | 종료 | |

### 13-4. 회원·승인 상태(기획 한글 → 코드 후보)

상세 코드 테이블은 DB 설계에서 확정. 여기서는 **용어 통일**만 적는다.

| 기획 한글(차단기업) | 기획 한글(직원) | 비고 |
|---------------------|-----------------|------|
| 대기, 정상, 사용정지 | 대기, 정상 | 운영 M4 필터와 동일 계열(00-04) |
| 승인·반려·승인취소 | (기업 파이프라인에 종속) | B1 승인관리 |

### 13-5. 카카오 알림 P코드 (기획서 ↔ 약어)

알림 종류 ID는 기획서 **P01~P11** 그대로 쓰고, 컬럼·VO에서는 §10-2 **`noti_tpl`** 계열과 매핑한다. 상황 설명은 [로드세이프티 00-04](../../../docs/00.프로젝트/00-04.로드세이프티-도메인용어-상태-P코드.md) 표가 정본이다.

| 코드 | 약어·저장 예 | 비고 |
|------|----------------|------|
| P01 ~ P11 | `noti_tpl_cd` = `P01` … | 발송 로그·감사 테이블과 연계 |

### 13-6. 초안 DB 네이밍(`mem_id` 등)과 본 사전

도메인 MD 초안에 `mem_id`, `constr_id` 등이 있으면 **의미는 유지**하되, **신규 스키마**는 `00-01` **PK `*_seq`**, **회원 `mber`**, **공사 `construction`/`constr`** 등 본 사전·§10·§10-2와 맞출 것.

| 초안(영문 관용) | 본 사전 권장 | 비고 |
|------------------|--------------|------|
| `mem_id` | `mber_seq` (회원 순번) | 공통 필드 `reg_seq`/`mod_seq`와 동일 계열 |
| `constr_id` | `construction_seq` 또는 도메인 합의 `constr_seq` | 팀이 짧은 식별자로 고정 후 일관 적용 |
| `order_corp_id` | `order_corp_seq` | 발주기업 PK |

> 이미 작성된 `docs/10.DB설계/*.md` 표기를 한 번에 바꾸지 않아도 된다. **새로 짜는 DDL·VO**부터 위 권장을 적용하면 된다.

---

## 14. 회원가입 도메인 용어 (추가)

회원가입 퍼블리싱(`register-*.html`)에서 도출된 신규 용어입니다.

### 14-1. 회원 계정 필드

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| 회원 아이디 | member identifier | mberId | `id` (§3) + `mber` (§6) 조합. HTML `name="mberId"` |
| 비밀번호 | password | mberPassword | `mber` + `password` (§8-1). HTML `name="mberPassword"` |
| 비밀번호 확인 | password confirm | mberPasswordConfirm | HTML `name="mberPasswordConfirm"` |
| 상호 | company name | companyNm | `nm` (§8-3) 조합. HTML `name="companyNm"` |

### 14-2. 기업 정보 필드

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| 대표자 전화번호 | ceo telephone | ceoTel | `ceo` (§10) + `tel` (§8-2). HTML `name="ceoTel1~3"` |
| 사업장 우편번호 | business postal code | bizPostCode | `biz` + `post_code` (§8-3). HTML `name="bizPostCode"` |
| 사업장 기본주소 | business address main | bizAddrMain | HTML `name="bizAddrMain"` |
| 사업장 상세주소 | business address detail | bizAddrDetail | HTML `name="bizAddrDetail"` |
| 사업자등록증 파일 | business registration file | bizRegFile | HTML `name="bizRegFile"` |
| 통장사본 파일 | bankbook file | bankFile | HTML `name="bankFile"` |
| SMS 수신 여부 | SMS receive yes/no | smsReceiveYn | `yn` (§4). HTML `name="smsReceiveYn"` |
| 이메일 수신 여부 | email receive yes/no | emailReceiveYn | `yn` (§4). HTML `name="emailReceiveYn"` |
| 이메일 아이디 | email identifier | emailId | HTML `name="emailId"` |
| 이메일 도메인 | email domain | emailDomain | HTML `name="emailDomain"` |

### 14-3. TMA 카드 필드

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| TMA 종류 | TMA type | tmaType | `tma` (§10-2) + `type` (§8-5). HTML `name="tmaType_N"` |
| TMA 차량번호 | TMA car number | tmaCarNo | `tma` + `car` + `no` (§3). HTML `name="tmaCarNo_N"` |
| TMA 소유주 | TMA owner | tmaOwner | `tma` + `owner` (§10). HTML `name="tmaOwner_N"` |
| TMA 파일 | TMA file | tmaFile | `tma` + `file` (§10). HTML `name="tmaFile_N"` |

### 14-4. 약관 동의 필드

| 한글명 | 영문명 | 영문약어 | 비고 |
|--------|--------|----------|------|
| 약관 동의 여부 | terms agree yes/no | termsAgreeYn | `yn` (§4). 체크박스 UI ID: `chkTerms1`, `chkTerms2`, `chkAll` |
| 가입 유형 | register type | regType | `reg` (§2) + `type` (§8-5). 값: `company` / `employee` (§12) |

> **camelCase 변환 규칙**: DB 컬럼명(`biz_post_code`)은 Java VO에서 `bizPostCode`로 변환한다.  
> `mber` 접두사는 회원 도메인 전용이며, 일반 사용자 UI 필드에도 동일하게 적용한다.
